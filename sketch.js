var database, form, player, game, gameState, playerCount;

function setup(){
    canvas = createCanvas(400,400);
    database = firebase.database();
    
}

function draw(){
    background("white");
    
}
